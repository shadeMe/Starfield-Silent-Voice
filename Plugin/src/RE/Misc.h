#pragma once

#include <RE\T\TESBoundObject.h>

namespace RE
{
	// Container for bits and bobs of different class that have 
	// yet to be fully decoded in CommonLib.
	namespace Misc
	{
		namespace TESBoundObject
		{
			bool IsNPCFemale(RE::TESBoundObject* Object);
		}
	}
}